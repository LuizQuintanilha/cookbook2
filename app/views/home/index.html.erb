<h2>Cookbook</h2>

<%= link_to 'Cadastrar Receita', new_recipe_path %>

<% if @recipes.any? %>
  <% @recipes.each do |r|%>
    <%= r.name %>
  <% end %>
<% else %>
  <p>Não existem receitas cadastradas</p>
<% end %>